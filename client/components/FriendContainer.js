import React from 'react';

const FriendContainer = (props) => {
  return (
    <div className='friend-container'>
      FriendContainer(details listed inside)
    </div>
  );
};

export default FriendContainer;
