import React from 'react';
import FriendContainer from './FriendContainer';

const ScoreBoard = (props) => {
  return <div id='score-board'>ScoreBoard!!</div>;
  <FriendContainer />;
};

export default ScoreBoard;
